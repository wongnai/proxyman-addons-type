# Proxyman Addons Type

Convert [type of Proxyman scripting](https://docs.proxyman.io/scripting/script) into Typescript.

In `tsconfig.json`, at `types` add `proxyman-addons-type`

In code `proxyman.addons.onRequest`
